# Marketing motion decision

## Status

Accepted for harness v2.

## Decision

An explicit current-user register or motion lock is authoritative before composition. Do not reinterpret, weaken, or replace it with inferred taste.

When the user has not supplied a register lock, marketing direction is autonomous: compare two or three distinct supported registers (**quiet**, **confident**, **showpiece**) against the lawful reference evidence and select the strongest eligible direction without asking the user to choose. The independent evaluator assesses the complete supplied set. Record exactly one evidence-backed rejection for every nonwinner.

This canonical register table is not a concept-study quota. Upstream visible studies may compare distinct concepts in the same register, or study one concept when current direction or visible evidence already resolved the uncertainty. Choose a representative for a register only after the visible concept comparison; never invent another concept to fill a third register. A canonical singleton requires a matching current explicit user register lock. A motion-only lock, an `explicit-user` label, or a convenience rationale does not authorize it. Visible-evidence-only canonical singletons remain unsupported until there is a trusted witness for that prior resolution; the current record has no such witness.

When one concept is already selected but expression strength remains uncertain, compare restrained and more-expressive realizations of that same concept while preserving its content-to-form invariants; these are register studies, not evidence of distinct concept inventions or cosmetic conceptual diversity. If neither a supported second register nor an explicit register lock exists, canonical singleton publication remains unsupported; do not invent evidence to fill the table.

Motion is an eligibility decision, not a decorative preference:

- `one` requires exactly one concept-bearing scene, one lawful high-visual-system reference, and either an approved recipe or a selected lawful positive motion observation.
- `none` requires no signature scene, a lawful high-visual-system static reference, a tried lawful CSS/SVG/static or reduced-motion fallback, and demonstrated static influence plus a template-breaking macro departure.
- Both decisions settle every pending motion slot. For `none`, reject every pending slot with a specific evidenced reason; `slots: []` is valid only when there are no pending slots.
- A generic desire for quiet, restraint, capability limits, category convention, anti-reference avoidance, fear of slop, or generic performance concern is not an escape hatch for `none`.

These are contextual evidence obligations, not an English vocabulary test. Review the actual
reason against the supplied observations: mentioning a category label, a capability or restraint
does not make a concrete visual finding generic. A nonwinning static candidate's rejection is not
the reason for omitting motion. Likewise, a required nonempty macro hypothesis does not need the
words `template`, `departure` or `break`. The machine validates the closed contract, authoritative
bindings and required dispositions; independent rendered review establishes whether the proposed
relationship and its departure are actually present. A claim that contains the preferred words is
not visual evidence, in any language.

The decision must pass blind-signature, narrative, motion-fit, fidelity/decision-fit, and macro-landing gates before publication.

## Consequences

The CLI exposes a read-only art-direction check. Final evidence v2 binds the selected decision and accepts only the eligible `none` or `one` evidence shape. Reference-distance telemetry remains advisory and cannot block publication.
