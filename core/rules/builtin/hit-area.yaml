id: HIT-002
layer: 1
category: a11y
severity: warn
when: "node.computed.isInteractive && !node.inline"
value: "node.computed.hitArea"
assert: "value.w >= 24 && value.h >= 24"
message: "Hit area {value} is smaller than the WCAG 2.2 AA 24x24 minimum"
