# Navigation and responsive action

Pass only when all are true:

- The first viewport offers a visible project-selection action at both desktop 1280x900 and
  mobile 390x844; it is not hidden below the fold or hover-only.
- Each case study has a reachable return-to-projects action and a next-case action whose
  labels predict the destination.
- Native links/buttons, keyboard focus, mobile target reach, and reduced-motion behavior are
  present and supported by sharp desktop/mobile evidence.
- The two supplied claims have distinct roles rather than competing as duplicate hero claims.
