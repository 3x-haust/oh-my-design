# Fact and message fidelity

Pass only when all are true:

- The fact ledger records 200 -> 5,000 concurrent users and Lighthouse 31 -> 96 as verified
  facts sourced to the brief; shipped claims reference those IDs.
- No unsupported employer, customer, award, revenue, testimonial, certification, or metric
  appears in shipped copy.
- Each H3 surface block has exactly one Main message, Supporting fact, Next action, and
  strict Claim refs field without repeating the title in the body and CTA.
- The hero does not narrate document structure or design process (for example,
  “과정을 남깁니다” or “순서대로 씁니다”).
