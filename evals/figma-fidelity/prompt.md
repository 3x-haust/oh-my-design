Implement the Figma design at this link as a production-ready web page. Use oh-my-design:figma.

Figma file: https://www.figma.com/design/PLACEHOLDER_KEY/Sample-Design-System

The file contains a landing page ("Hero" frame at 1440px and 375px), a features section ("Features" frame at 1440px), and a shared Button component set with Size=sm/md and State=default/hover variants. Build the full page using the extracted design system tokens, implement the Button component once and reuse it, run the pixel diff loop for each frame, and ship with a fidelity report.
