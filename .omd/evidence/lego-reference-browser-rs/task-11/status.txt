Todo 11 remediation status

worktree: <repository-worktree>
branch: feat/lego-reference-browser-rs

Completed remediation
- Removed the human-only reference-assembly table parser/assertion without adding a replacement
  prompt-prose test.
- Removed the image-first natural-language regex test while retaining the unrelated parallelism
  checks.
- Reconciled the coordinator/composer prompt topology: the coordinator/host alone derives directions
  and prompts from the selected sanitized assembly and project-owned inputs; it generates 2–3
  independent drafts concurrently, chooses one, manages the generation cache, and records/checks
  lineage. Composer starts only after checked selected or unavailable lineage, then analyzes and
  translates the selected draft or uses the CSS/SVG route.
- Kept raw source URLs, screenshots, pixels, visual likeness, and undeclared input carriers outside
  clean-room generation and composer inputs. Provider/cache lineage remains internal.

Verification
- Focused command: 33 tests, 33 pass, 0 fail.
- One remediation `npm run build`: exit 0. Raw stdout/stderr is retained in
  `build-remediation-raw.log`.
- Full `npm test`: 1,351 tests, 1,350 pass, 0 fail, 1 expected platform skip. Raw stdout/stderr is
  retained in `full-test-remediation-raw.log`.
- `npx tsc --noEmit`: exit 0.
- `git diff --check`: exit 0.
- Source/generated byte parity: root 22/22, Codex 69/69, Claude 63/63; all equal.
- Root, Codex, and Claude MCP manifests each select exactly `browser-rs`; no shipped
  `chrome-devtools` or `chrome-devtools-mcp` reference remains.

Result: PASS.
