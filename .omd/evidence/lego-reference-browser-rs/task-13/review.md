# Task 13 review

Each trusted input is a lstat/realpath/read/lstat snapshot carrying exact bytes plus device, inode, size, mtime, and ctime identity. Validation samples board, selection, lineage, attribution, and usage before and after parsing; it samples evidence before and after each row; it brackets artifact and lineage checks with the same input generation; and it retries no more than three times before failing closed. After all evidence checks, it invokes the deterministic post-evidence seam, captures board/raw assembly, selection, checked lineage, and attribution again, re-samples usage where applicable, and re-reads every evidence snapshot. The final samples must exactly match the initial byte identities before either a validated result is returned or a usage record can be written. Artifact manifests, canonical raw-board/assembly bytes, parsed lineage bytes, selection hashes, and lineage selection/assembly bindings must all agree, preventing mixed generations and ABA replacement identities.

Rows are closed exact-selected-piece mappings. Every `used`, `rejected`, and `anti-reference` row requires a non-empty `nonBorrowedProperties` list; rejected and anti-reference rows additionally require empty borrowed properties. Free text rejects source, Markdown, HTML, C0/C1, ALM/LRM/RLM, bidi, pipe, and backtick carriers. The report is rendered from validated values through deterministic inert plain-cell normalization: links, images, autolinks, HTML brackets, punctuation carriers, controls, directional marks, whitespace, and pipes cannot become active Markdown or HTML.

Pure LOC: files 81, parser 123, snapshot 171, report 76, public usage API 15. Every production module remains below 250 lines. Focused tests and TypeScript are clean; the final adjacent gate is recorded in `green.md`. A scoped explicit-`any` scan returned no matches. No build was run, per task scope.
